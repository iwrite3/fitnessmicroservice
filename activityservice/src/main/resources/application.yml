spring:
  application:
    name: activity-service
  config:
    # Inject the cloud Config Server URL, fallback to local for laptop testing
    import: configserver:${CONFIG_SERVER_URL:http://localhost:8888}
